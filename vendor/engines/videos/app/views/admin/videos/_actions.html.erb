<ul>
  <% if Admin::VideosController.searchable? %>
    <li>
      <%= render :partial => "/shared/admin/search",
                  :locals => {
                    :url => admin_videos_url
                  } %>
    </li>
  <% end %>
  <li>
    <%= link_to t('.create_new'), new_admin_video_url,
                 :class => "add_icon" %>
  </li>
<% if !searching? and Admin::VideosController.sortable? and Video.count > 1 %>
  <li>
    <%= link_to t('.reorder', :what => "Videos"),
                 admin_videos_url,
                 :id => "reorder_action",
                 :class => "reorder_icon" %>

    <%= link_to t('.reorder_done', :what => "Videos"),
                 admin_videos_url,
                 :id => "reorder_action_done",
                 :style => "display: none;",
                 :class => "reorder_icon" %>
  </li>
<% end %>
</ul>